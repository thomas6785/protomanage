from setuptools import setup, find_packages

setup(
    name="protomanage",
    version="0.dev0",
    python_requires=">=3.12",
    package_dir={"": "."},
    install_requires=[
        # Add dependencies here if needed
    ],
)
